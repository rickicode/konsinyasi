---
title: 'Phase A: Bootstrap & Tooling — Scaffold'
type: task
permalink: konsinyasi/tasks/phase-a-bootstrap-tooling-scaffold
status: done
current_step: 2
steps:
  - Create empty directory structure from docs/FRONTEND_ARCHITECTURE.md
  - Add .gitkeep to every empty directory for Git tracking
  - Verify no files other than .gitkeep were placed in empty leaf directories
---

# Phase A: Bootstrap & Tooling — Scaffold

Scaffold the empty source tree required by `docs/FRONTEND_ARCHITECTURE.md` for the `rewrite/ultracode-frontend` branch.

## Context

- Feature-folders architecture under `src/web/features/`.
- Shared code under `src/shared/` and cross-feature code under `src/web/shared/`.
- Empty directories need `.gitkeep` so scaffold commits remain complete.
